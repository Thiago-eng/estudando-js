let logado = false
let moderador = true
let banido = false

if (logado === true && banido === false || moderador === true && banido === false) {
    console.log("Pode logar no sistema")
} else {
    console.log("Não pode logar no sistema")
}

// Eu quis apenas aproveitar a chance para colocar essa opção alternativa anterior.